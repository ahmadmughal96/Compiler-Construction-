#include "input_scanner.h"                                                                                                                            int main(){	float area, volume;                           	int radius = 5;                                            int num;	float X = -44.15;	float Y = 416.0e+1;	float Z = X / Y;                                                           	for (num = 0; num < 10; num++){		printf("The Number is: %d\n", num);                                                   	}	area = PI * radius * radius;                                                           volume = (4.0/3.0) * PI * radius * radius * radius;                        	printf("Area: %f, Volume: %f\n", area, volume);    return 0;                               }